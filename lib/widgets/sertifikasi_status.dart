import 'package:flutter/material.dart';

class SertifikasiStatus extends StatelessWidget {
  const SertifikasiStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Tab Status"),
    );
  }
}
